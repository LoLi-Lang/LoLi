(def .. (\ (start end)
	(if (eq? start end)
		(cons end nil)
		(cons start (.. (add1 start) end)))))

(def nil? (\ (obj)
	(if (eq? nil obj)
		t
		nil)))

(def append (\ (lsta lstb)
	(if (nil? lsta)
		lstb
		(cons (head lsta) (append (tail lsta) lstb)))))
